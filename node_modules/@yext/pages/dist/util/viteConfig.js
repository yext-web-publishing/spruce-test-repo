import { existsSync } from "node:fs";
import path from "node:path";
const VITE_CONFIG = "vite.config.js";
const scopedViteConfigPath = (scope) => {
  if (scope) {
    const scopedViteConfigPath2 = path.resolve(scope, VITE_CONFIG);
    if (existsSync(scopedViteConfigPath2)) {
      return scopedViteConfigPath2;
    }
  }
  const viteConfigPath = path.resolve(VITE_CONFIG);
  if (existsSync(viteConfigPath)) {
    return viteConfigPath;
  }
};
export {
  scopedViteConfigPath
};
