import { RedirectModuleInternal } from "../../../../common/src/redirect/internal/types.js";
import { Manifest, TemplateProps } from "../../../../common/src/template/types.js";
import { ProjectStructure } from "../../../../common/src/project/structure.js";
import { RedirectSource } from "../../../../common/src/redirect/types.js";
export type GeneratedRedirect = {
    sources: RedirectSource[];
    destination: string;
};
/**
 * @returns an array of redirect modules matching the document's feature.
 */
export declare const readRedirectModules: (feature: string, manifest: Manifest, projectStructure: ProjectStructure) => Promise<RedirectModuleInternal<any> | undefined>;
/**
 * Takes in both a redirect module and its stream document, processes them, and writes them to disk.
 *
 * @param redirectModuleInternal
 * @param templateProps
 */
export declare const generateRedirectResponses: (redirectModuleInternal: RedirectModuleInternal<any>, templateProps: TemplateProps) => Promise<GeneratedRedirect>;
