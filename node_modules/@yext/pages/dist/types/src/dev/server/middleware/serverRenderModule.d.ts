import { RequestHandler } from "express-serve-static-core";
import { ViteDevServer } from "vite";
type Props = {
    vite: ViteDevServer;
    modulePath: string;
};
export declare const serverRenderModule: ({ vite, modulePath }: Props) => RequestHandler;
export {};
