import * as ReactDOMServer from "react-dom/server";
import * as React from "react";
const render = async (pageContext) => {
  const { Page } = pageContext;
  return ReactDOMServer.renderToString(/* @__PURE__ */ React.createElement(Page, null));
};
const replacementTag = "<!--YEXT-SERVER-->";
const indexHtml = `<!DOCTYPE html>
    <html>
      <head></head>
      <body>
        <div id="reactele">${replacementTag}</div>
      </body>
    </html>`;
export {
  indexHtml,
  render,
  replacementTag
};
