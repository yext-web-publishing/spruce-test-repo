const validateModuleInternal = (module) => {
  if (!module.default) {
    throw new Error(
      `Module ${module.filename} must have a React component as a default export.`
    );
  }
};
export {
  validateModuleInternal
};
