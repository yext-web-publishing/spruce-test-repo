import * as ReactDOM from "react-dom";
import * as React from "react";
const render = async (moduleContext) => {
  const { Page } = moduleContext;
  ReactDOM.hydrate(/* @__PURE__ */ React.createElement(Page, null), document.getElementById("reactele"));
};
export {
  render
};
