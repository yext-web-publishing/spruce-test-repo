import * as ReactDOM from "react-dom/client";
import * as React from "react";
const render = async (moduleContext) => {
  const { Page } = moduleContext;
  ReactDOM.hydrateRoot(document.getElementById("reactele"), /* @__PURE__ */ React.createElement(Page, null));
};
export {
  render
};
