import * as ReactDOMServer from "react-dom/server";
import * as React from "react";
const render = async (pageContext) => {
  const { Page, pageProps } = pageContext;
  return ReactDOMServer.renderToString(/* @__PURE__ */ React.createElement(Page, { ...pageProps }));
};
const replacementTag = "<!--YEXT-SERVER-->";
const indexHtml = `<!DOCTYPE html>
    <html lang="<!--app-lang-->">
      <head></head>
      <body>
        <div id="reactele">${replacementTag}</div>
      </body>
    </html>`;
export {
  indexHtml,
  render,
  replacementTag
};
